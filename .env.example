# Database Configuration
# Required: PostgreSQL connection string
# For local development, you can use a local PostgreSQL instance or a cloud service like Neon
# Example: postgresql://user:password@localhost:5432/aurora_db
# Example (Neon): postgresql://user:password@ep-xxx.region.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://postgres:Abcd1234@127.0.0.1:5432/aurora_db

# Server Configuration
# Optional: Port number (defaults to 6000)
PORT=6000

# Session Configuration
# Optional: Secret key for session encryption (change in production!)
# Default: "aurora-my-secret-key-change-in-production"
SESSION_SECRET=aurora-my-secret-key-change-in-production

# Environment
# Optional: Set by npm scripts (development/production)
NODE_ENV=development

# Replit Configuration (Optional - only needed if running on Replit)
# REPL_ID=
